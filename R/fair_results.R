# F-UJI
#
# A Service for Evaluating Research Data Objects Based on <a href ='https://doi.org/10.5281/zenodo.3775793'>FAIRsFAIR Metrics</a>. <p> This work was supported by the <a href='https://www.fairsfair.eu/'>FAIRsFAIR</a> project (H2020-INFRAEOSC-2018-2020 Grant Agreement 831558).
#
# The version of the OpenAPI document: 1.1.0
# Contact: anusuriya.devaraju@googlemail.com
# Generated by: https://openapi-generator.tech

#' @docType class
#' @title FAIRResults
#'
#' @description FAIRResults Class
#'
#' @format An \code{R6Class} generator object
#'
#' @field test_id  character [optional]
#'
#' @field request  named list( object ) [optional]
#'
#' @field timestamp  character [optional]
#'
#' @field expiry_timestamp  character [optional]
#'
#' @field metric_specification  character [optional]
#'
#' @field metric_version  character [optional]
#'
#' @field software_version  character [optional]
#'
#' @field total_metrics  integer [optional]
#'
#' @field summary  named list( object ) [optional]
#'
#' @field results  list( \link{} ) [optional]
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
FAIRResults <- R6::R6Class(
  'FAIRResults',
  public = list(
    `test_id` = NULL,
    `request` = NULL,
    `timestamp` = NULL,
    `expiry_timestamp` = NULL,
    `metric_specification` = NULL,
    `metric_version` = NULL,
    `software_version` = NULL,
    `total_metrics` = NULL,
    `summary` = NULL,
    `results` = NULL,
    initialize = function(
        `test_id`=NULL, `request`=NULL, `timestamp`=NULL, `expiry_timestamp`=NULL, `metric_specification`=NULL, `metric_version`=NULL, `software_version`=NULL, `total_metrics`=NULL, `summary`=NULL, `results`=NULL, ...
    ) {
      local.optional.var <- list(...)
      if (!is.null(`test_id`)) {
        stopifnot(is.character(`test_id`), length(`test_id`) == 1)
        self$`test_id` <- `test_id`
      }
      if (!is.null(`request`)) {
        stopifnot(is.vector(`request`), length(`request`) != 0)
        sapply(`request`, function(x) stopifnot(is.character(x)))
        self$`request` <- `request`
      }
      if (!is.null(`timestamp`)) {
        stopifnot(is.character(`timestamp`), length(`timestamp`) == 1)
        self$`timestamp` <- `timestamp`
      }
      if (!is.null(`expiry_timestamp`)) {
        stopifnot(is.character(`expiry_timestamp`), length(`expiry_timestamp`) == 1)
        self$`expiry_timestamp` <- `expiry_timestamp`
      }
      if (!is.null(`metric_specification`)) {
        stopifnot(is.character(`metric_specification`), length(`metric_specification`) == 1)
        self$`metric_specification` <- `metric_specification`
      }
      if (!is.null(`metric_version`)) {
        stopifnot(is.character(`metric_version`), length(`metric_version`) == 1)
        self$`metric_version` <- `metric_version`
      }
      if (!is.null(`software_version`)) {
        stopifnot(is.character(`software_version`), length(`software_version`) == 1)
        self$`software_version` <- `software_version`
      }
      if (!is.null(`total_metrics`)) {
        stopifnot(is.numeric(`total_metrics`), length(`total_metrics`) == 1)
        self$`total_metrics` <- `total_metrics`
      }
      if (!is.null(`summary`)) {
        stopifnot(is.vector(`summary`), length(`summary`) != 0)
        sapply(`summary`, function(x) stopifnot(is.character(x)))
        self$`summary` <- `summary`
      }
      if (!is.null(`results`)) {
        stopifnot(is.vector(`results`), length(`results`) != 0)
        sapply(`results`, function(x) stopifnot(R6::is.R6(x)))
        self$`results` <- `results`
      }
    },
    toJSON = function() {
      FAIRResultsObject <- list()
      if (!is.null(self$`test_id`)) {
        FAIRResultsObject[['test_id']] <-
          self$`test_id`
      }
      if (!is.null(self$`request`)) {
        FAIRResultsObject[['request']] <-
          self$`request`
      }
      if (!is.null(self$`timestamp`)) {
        FAIRResultsObject[['timestamp']] <-
          self$`timestamp`
      }
      if (!is.null(self$`expiry_timestamp`)) {
        FAIRResultsObject[['expiry_timestamp']] <-
          self$`expiry_timestamp`
      }
      if (!is.null(self$`metric_specification`)) {
        FAIRResultsObject[['metric_specification']] <-
          self$`metric_specification`
      }
      if (!is.null(self$`metric_version`)) {
        FAIRResultsObject[['metric_version']] <-
          self$`metric_version`
      }
      if (!is.null(self$`software_version`)) {
        FAIRResultsObject[['software_version']] <-
          self$`software_version`
      }
      if (!is.null(self$`total_metrics`)) {
        FAIRResultsObject[['total_metrics']] <-
          self$`total_metrics`
      }
      if (!is.null(self$`summary`)) {
        FAIRResultsObject[['summary']] <-
          self$`summary`
      }
      if (!is.null(self$`results`)) {
        FAIRResultsObject[['results']] <-
          lapply(self$`results`, function(x) x$toJSON())
      }

      FAIRResultsObject
    },
    fromJSON = function(FAIRResultsJson) {
      FAIRResultsObject <- jsonlite::fromJSON(FAIRResultsJson)
      if (!is.null(FAIRResultsObject$`test_id`)) {
        self$`test_id` <- FAIRResultsObject$`test_id`
      }
      if (!is.null(FAIRResultsObject$`request`)) {
        self$`request` <- ApiClient$new()$deserializeObj(FAIRResultsObject$`request`, "map(object)", loadNamespace("rfuji"))
      }
      if (!is.null(FAIRResultsObject$`timestamp`)) {
        self$`timestamp` <- FAIRResultsObject$`timestamp`
      }
      if (!is.null(FAIRResultsObject$`expiry_timestamp`)) {
        self$`expiry_timestamp` <- FAIRResultsObject$`expiry_timestamp`
      }
      if (!is.null(FAIRResultsObject$`metric_specification`)) {
        self$`metric_specification` <- FAIRResultsObject$`metric_specification`
      }
      if (!is.null(FAIRResultsObject$`metric_version`)) {
        self$`metric_version` <- FAIRResultsObject$`metric_version`
      }
      if (!is.null(FAIRResultsObject$`software_version`)) {
        self$`software_version` <- FAIRResultsObject$`software_version`
      }
      if (!is.null(FAIRResultsObject$`total_metrics`)) {
        self$`total_metrics` <- FAIRResultsObject$`total_metrics`
      }
      if (!is.null(FAIRResultsObject$`summary`)) {
        self$`summary` <- ApiClient$new()$deserializeObj(FAIRResultsObject$`summary`, "map(object)", loadNamespace("rfuji"))
      }
      if (!is.null(FAIRResultsObject$`results`)) {
        self$`results` <- ApiClient$new()$deserializeObj(FAIRResultsObject$`results`, "array[]", loadNamespace("rfuji"))
      }
      self
    },
    toJSONString = function() {
      jsoncontent <- c(
        if (!is.null(self$`test_id`)) {
        sprintf(
        '"test_id":
          "%s"
                ',
        self$`test_id`
        )},
        if (!is.null(self$`request`)) {
        sprintf(
        '"request":
          "%s"
        ',
        jsonlite::toJSON(lapply(self$`request`, function(x){ x }), auto_unbox = TRUE, digits=NA)
        )},
        if (!is.null(self$`timestamp`)) {
        sprintf(
        '"timestamp":
          "%s"
                ',
        self$`timestamp`
        )},
        if (!is.null(self$`expiry_timestamp`)) {
        sprintf(
        '"expiry_timestamp":
          "%s"
                ',
        self$`expiry_timestamp`
        )},
        if (!is.null(self$`metric_specification`)) {
        sprintf(
        '"metric_specification":
          "%s"
                ',
        self$`metric_specification`
        )},
        if (!is.null(self$`metric_version`)) {
        sprintf(
        '"metric_version":
          "%s"
                ',
        self$`metric_version`
        )},
        if (!is.null(self$`software_version`)) {
        sprintf(
        '"software_version":
          "%s"
                ',
        self$`software_version`
        )},
        if (!is.null(self$`total_metrics`)) {
        sprintf(
        '"total_metrics":
          %d
                ',
        self$`total_metrics`
        )},
        if (!is.null(self$`summary`)) {
        sprintf(
        '"summary":
          "%s"
        ',
        jsonlite::toJSON(lapply(self$`summary`, function(x){ x }), auto_unbox = TRUE, digits=NA)
        )},
        if (!is.null(self$`results`)) {
        sprintf(
        '"results":
        [%s]
',
        paste(sapply(self$`results`, function(x) jsonlite::toJSON(x$toJSON(), auto_unbox=TRUE, digits = NA)), collapse=",")
        )}
      )
      jsoncontent <- paste(jsoncontent, collapse = ",")
      paste('{', jsoncontent, '}', sep = "")
    },
    fromJSONString = function(FAIRResultsJson) {
      FAIRResultsObject <- jsonlite::fromJSON(FAIRResultsJson)
      self$`test_id` <- FAIRResultsObject$`test_id`
      self$`request` <- ApiClient$new()$deserializeObj(FAIRResultsObject$`request`, "map(object)", loadNamespace("rfuji"))
      self$`timestamp` <- FAIRResultsObject$`timestamp`
      self$`expiry_timestamp` <- FAIRResultsObject$`expiry_timestamp`
      self$`metric_specification` <- FAIRResultsObject$`metric_specification`
      self$`metric_version` <- FAIRResultsObject$`metric_version`
      self$`software_version` <- FAIRResultsObject$`software_version`
      self$`total_metrics` <- FAIRResultsObject$`total_metrics`
      self$`summary` <- ApiClient$new()$deserializeObj(FAIRResultsObject$`summary`, "map(object)", loadNamespace("rfuji"))
      self$`results` <- ApiClient$new()$deserializeObj(FAIRResultsObject$`results`, "array[]", loadNamespace("rfuji"))
      self
    }
  )
)
